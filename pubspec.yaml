name: panda_garden
description: A Flutter app for managing panda garden visits

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  shared_preferences: ^2.2.0
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.0.5

  # Local Notifications
  flutter_local_notifications: ^16.3.0

  # UI Components
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Assets (if you want to add images/fonts later)
  # assets:
  #   - assets/images/
  #   - assets/icons/

  # Fonts (optional custom fonts)
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf
  #       - asset: fonts/CustomFont-Bold.ttf
  #         weight: 700
